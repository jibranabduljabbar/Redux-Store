const INITIAL_STATE = {
    name: "Admin",
    role: "admin",
    email: "admin@redux.com",
    code: 786,
    password: "admin786123",
}

export default (state = INITIAL_STATE) => {
    return state;
}